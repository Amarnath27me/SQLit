[phases.setup]
nixPkgs = ["python312", "gcc", "postgresql.lib"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8001} --workers 1 --log-level info"
